# ─────────────────────────────────────────────────────────
# ⚠️ 竞品分析产物 —— 绝不入库
#
# 含 FlOZE 官方 APK、解包后的 DEX、资源文件。这些是第三方版权
# 作品，上传到公开仓库属于再分发，会被 DMCA 下架；也与「展示
# 自己的开发能力」这一目的无关。
# ─────────────────────────────────────────────────────────
floze_analysis/
floze.apk

# 竞品 API 契约（逆向产物）与分析报告
# —— 公开仓库发布对商业产品的逆向结果有法律风险，且不是本项目的
#    开发成果。需要给审核方看的话，单独私下提供。
floze_reverse/api_contract.txt
Floze竞品技术分析报告.md

# 逆向探针脚本（含对其他软件 CMUX / Typora 的操作，与本项目无关）
scripts/

# 第三方软件安装包
*.dmg
cmux-macos.dmg
Typora.dmg

# ─────────────────────────────────────────────────────────
# 工具链 —— 1GB，且可由脚本重新下载
# ─────────────────────────────────────────────────────────
toolchain/

# ─────────────────────────────────────────────────────────
# Agent 工作记录 —— 内部会话状态，非项目内容
# ─────────────────────────────────────────────────────────
.codewhale/

# ─────────────────────────────────────────────────────────
# 构建产物
# ─────────────────────────────────────────────────────────
android_shell/out/
android_shell/app/build/
*.apk
*.aab
*.ap_
*.dex
*.class
__pycache__/
*.py[cod]

# ─────────────────────────────────────────────────────────
# 签名密钥 —— 即使 debug keystore 也不入库
# ─────────────────────────────────────────────────────────
*.keystore
*.jks
*.p12
android_shell/debug.keystore.bak

# ─────────────────────────────────────────────────────────
# 运行时数据 —— 含真实聊天记录与用户信息
# ─────────────────────────────────────────────────────────
floze_clone/floze.db
floze_clone/floze.db-shm
floze_clone/floze.db-wal

# 运行产物（可由脚本重新生成）
floze_clone/static/speech/
floze_clone/static/download/

# ─────────────────────────────────────────────────────────
# 凭证 —— 绝不入库
# ─────────────────────────────────────────────────────────
.env
.env.*
.minimax_key
*_key
credentials*.json
service-account*.json

# ─────────────────────────────────────────────────────────
# 系统与编辑器
# ─────────────────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
*.swp

# ─────────────────────────────────────────────────────────
# 探针缓存
# ─────────────────────────────────────────────────────────
.probe/
