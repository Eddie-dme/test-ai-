# AI 角色扮演 / 陪伴类产品 — 三方模型选型建议

> **背景**：为 Floze 类产品（AI 角色扮演 + 情感陪伴 + 长记忆 + 叙事创作）做技术选型
> **日期**：2026-09-20
> **声明**：本文价格与市场信息来自公开网页检索，**模型市场变动极快，落地前请重新核价**。FlOZE 自身所用模型**无法从客户端确证**（见 `Floze竞品技术分析报告.md` §8）。

---

## 0. 一句话结论

**这个品类的第一约束不是模型能力，而是内容审核政策（AUP）。**

一线厂商（OpenAI / Anthropic / Google / Together / Fireworks）的 AUP **明确禁止露骨性内容**，因此在 Mature 17+ 的情感陪伴场景中直接出局——**不是价格问题，是政策问题**。

实际可用的组合是三档：**中国系大模型 + 专门 RP 模型 + 自部署开源**。

---

## 1. 这个品类对模型的真实需求

按权重排序（与通用聊天产品差异很大）：

1. **内容尺度容忍度** — 硬门槛，一票否决
2. **人设一致性** — 长会话不"出戏"，是体验核心
3. **长上下文 + 记忆召回** — 1M 级上下文，配合 RAG
4. **Prompt caching** — 角色卡 + 记忆前缀复用，**成本关键**
5. **风格化文学输出** — Romantasy 调性，而非通用助手腔
6. **流式首字延迟** — 对话体感
7. **多语言** — 英 → 中/日/繁中扩展
8. **成本分层能力** — 支撑 hearts 分档（Lite/Classic/Epic）

> 注意：**通用能力（MMLU 等）在该品类权重很低**。一个 MMLU 更低但 RP 微调过的模型，体验会显著更好。

---

## 2. 行业成本基准（竞品实测估算）

| 产品 | 模型策略 | 单条消息成本 |
|---|---|---|
| Character.AI | 自研 in-house | ~$0.0015 |
| Talkie | MiniMax 自研 | ~$0.0005 |
| Chai | 自研优化模型 | ~$0.0002 |
| **FlOZE** | **自建后端，客户端无任何三方 LLM SDK** | 未知 |

*数据来源：a16z《The AI Companion Market》、Sacra 公司档案（外部估算，非审计披露）*

**关键观察**：**头部产品全部自研或使用母公司的自研模型，没有一家直接用三方通用 API。** 这既解释了 FlOZE 的选择，也说明三方路线在成本与内容控制上确有短板——但**对复刻方而言，三方仍是快速验证的最优路径**。

---

## 3. 三方候选分层

### 档位 A — 主力对话模型（推荐）

**MiniMax（最贴合）**

- 已有**专为角色扮演训练的模型**：`M2-her`，官方定位"沉浸式角色扮演与人设驱动聊天，支持多轮对话并保持语调性格一致性"（2026-01 上线，自有平台 + OpenRouter 均可用）
- **Talkie 即 MiniMax 自研产品**——等于可以直接调用经过亿级用户验证的同源能力
- 通用档定价（2026-06）：M3 / M2.7 约 **$0.30 / $1.20** per 1M（in/out），**cache read 仅 $0.06**（对长记忆场景极关键）
- 风险：**既是对手又是供应商**，需评估供应链风险

**DeepSeek 系**

- OpenRouter 角色扮演用量榜前列，且**价格最低档**：约 **$0.04–0.13 / $0.08–0.52** per 1M
- 1M+ 上下文，适合长会话

**智谱 GLM（z-ai）**

- RP 榜用量前列，约 **$0.075 / $0.25** per 1M，部分时段有折扣

**Qwen（阿里）**

- Apache 2.0 开源可自部署；**中文能力最强**，若做中文/繁中市场优先

**腾讯混元 / 小米 MiMo**

- RP 榜上有一定用量（混元约 $0.834/$2.501，小米约 $0.119/$0.238），可作为备选冗余

> 共同特征：**中国系模型在 RP 场景天然尺度更宽**，且普遍提供 1M 级上下文与 prompt caching。

### 档位 B — 聚合器（快速试水）

**OpenRouter**

- 500+ 模型统一入口，按 token 计费 + **5.5% 平台费**
- 适合：**同时试多家、快速做 A/B**
- 陷阱：存在**平台级 moderation transform**，且上游 provider 仍可能过滤；必须配置 model/provider allowlist 并逐个核对上游 AUP

### 档位 C — 包月制（小规模验证）

**Featherless**：$10–75/月，无限 token，25,000+ 模型
- 适合：**冷启动期压成本**、内部评测
- 不适合：规模化生产（质量与 SLA 不可控）

**Infermatic / Arli AI**：主打 uncensored RP，但社区反馈**性价比一般**，需自行验证

### 档位 D — 自部署（成本与内容的终极控制）

**Llama 系**（社区 RP 微调生态最丰富）· **Mistral**（Apache 2.0，MoE 成本优势）· **Qwen**（开源 + 中文）

适用条件：
- 需要**内容完全可控**（含尺度边界自定）
- 需要**数据不出境 / 不被用于训练**
- 规模足够大到租 GPU 比买 token 便宜

---

## 4. 推荐架构：分层路由

对齐 FlOZE 的 Lite / Quick / Smooth / Classic / Sparkline 分档：

```
┌─ Epic（长输出、高价值）      → MiniMax M2-her / 高档模型
├─ Classic / Smooth（主力）    → MiniMax M2-her / DeepSeek
├─ Lite / Quick（免费用户）    → 最低价档（DeepSeek 低价 / GLM）
└─ 兜底（降级 / 限流 / 合规）   → 自部署开源模型
```

**三条工程要点**：

1. **必须做 prompt caching**——角色卡 + 记忆摘要 + 历史前缀是固定大头，缓存命中可把成本压到 1/5（MiniMax cache read $0.06 vs 正常 $0.30）
2. **模型档位与 hearts 消耗挂钩**——与 FlOZE 一致，让免费用户走便宜模型，高档消耗走贵模型
3. **多供应商冗余**——单一供应商有政策反转、涨价、限流风险，至少两家 + 一处自部署兜底

---

## 5. 成本粗算（供决策）

按一条消息 **input ≈ 8k tokens**（角色卡 + 记忆 + 历史）、**output ≈ 300 tokens** 估算：

```
低价档（$0.04/$0.08）：
  8000 × 0.04/1M + 300 × 0.08/1M  = $0.00032 + $0.000024 ≈ $0.00034 /条

MiniMax 档（$0.30/$1.20）：
  8000 × 0.30/1M + 300 × 1.20/1M  = $0.0024 + $0.00036  ≈ $0.00276 /条
```

对比行业基准（Chai $0.0002、Talkie $0.0005）可见：**低价档模型的三方调用成本已接近头部自研水平**；高价档则明显高于行业基准——**这正是必须做分层路由与缓存的原因**。

> 该测算未含输出长尾（Epic 模式 1500 字符 ≈ 750+ tokens）与重试开销，实际应按 P95 而非均值估算。

---

## 6. 合规红线（不可让渡）

- **CSAM 零容忍**——与内容尺度是两件事，必须服务端拦截 + 年龄门禁
- **年龄验证**——Mature 17+ 分级要求，且直接影响渠道与投放地区
- **数据留存与训练**——需与企业级协议确认"不用于训练、可零留存"
- **区域法规**——EU AI Act 透明度义务、美国各州（如涉及）未成年保护法
- **供应商 AUP 与实际行为的一致性**——签约前用小样本实测，不要只看条款

---

## 7. 选型验证方法（落地前必做）

自建评测集，**不要只看榜单**：

1. **人设一致性**——50+ 轮长会话后人设漂移程度
2. **记忆召回**——跨会话引用早期剧情事件的准确率
3. **尺度边界**——在合规范围内可达的表达上限
4. **首字延迟**——流式 P50/P95
5. **成本压测**——按真实 token 分布（含长尾）算 P95 成本
6. **拒绝率**——同等提示下的 soft refusal 频率（**这是 RP 体验的隐形杀手**）

---

## 8. 直接结论

| 场景 | 推荐 |
|---|---|
| 英文市场、Mature 17+ 的 RP 陪伴 | **MiniMax M2-her 为主力**（专为 RP 训练 + Talkie 同源验证） |
| 成本敏感 / 免费用户档 | **DeepSeek 低价档 / GLM** |
| 中文 / 繁中市场 | **Qwen 系**（中文最强 + 可自部署） |
| 快速试多家 | **OpenRouter**（注意 moderation 与 5.5% 费率） |
| 规模上量后的成本与内容控制 | **自部署 Llama / Qwen 微调** |
| 一线通用 API（OpenAI/Anthropic/Google） | **不适用**（AUP 禁止，非价格问题） |

**最终建议**：以 **MiniMax M2-her 或 DeepSeek 档** 起步验证产品模型，同步储备一套自部署开源方案作为成本与合规兜底；等单日消息量稳定后再决定是否全面自建——**FlOZE 走的就是这条路**。

---

*价格与市场信息来自 2026-09 公开网页检索（OpenRouter、Featherless、a16z、Sacra、MiniMax 开放平台等），模型市场变动频繁，落地前请重新核价并核对最新 AUP。*
